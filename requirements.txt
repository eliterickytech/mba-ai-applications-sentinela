# Instala o pacote Sentinela em modo editável (dependências definidas no pyproject.toml)
# + os extras para renderizar o relatório/slides (relatorio) e rodar os testes (dev).
# Um único comando prepara tudo:  pip install -r requirements.txt
-e .[relatorio,dev]
